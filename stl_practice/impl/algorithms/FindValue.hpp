/*
 * This file include all the self-written algorithms 
 * for finding different values in some sequence
 */
#include <vector>

namespace Algorithms
{
   //find a absent value in sequence of numbers(from 1 to N)
   int findAbsentValInSqns(std::vector<int>& v);
   // find a not duplacate value in a vector
   int findAbsentDuplicate(std::vector<int>& v);
}